version: '2'
services:
  nginx-proxy:
    image: nginx
    container_name: nginx-wator
    network_mode: "host"
    volumes:
      - ./nginx/certs/www.wator.xyz:/etc/certs:ro
      - ./nginx/conf/conf.d:/etc/nginx/conf.d
    restart: always
